#include<vector>
#include<iostream>

using namespace std;

void permute(vector<int>& nums) 
{
        
}





int main()
{
    vector<int> nums;
    nums.push_back(1);
    nums.push_back(2);
    nums.push_back(3);
    permute(nums);
}